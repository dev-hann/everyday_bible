part of setting_repo;

class SettingImpl extends SettingRepo {
  @override
  dynamic loadSetting() {
    // return settingBox.loadSetting();
  }

  @override
  Future updateSetting(Map<String, dynamic> data) async {
    // return settingBox.updateSetting(data);
  }
}
